indico==3.3.12

# Flask is a transitive dependency of indico itself, but the flask-framework
# Rockcraft extension requires it to appear explicitly in requirements.txt.
flask

# Default plugin set baked into the rock at build time. Additional plugins can
# be installed at deploy time via the charm's `external_plugins` config.
indico-plugin-payment-paypal
indico-plugin-piwik
indico-plugin-storage-s3

# SAML support libraries (carried over from the indico-operator rock). xmlsec
# is compiled here at build time where the toolchain is available.
xmlsec==1.3.17
python3-saml

# Pinned to dodge https://github.com/boto/boto3/issues/4398 which breaks
# indico-plugin-storage-s3.
boto3==1.43.55

# Local CLI plugins (carried over from indico-operator) that back the
# add-admin and anonymize-user charm actions: `indico autocreate admin` and
# `indico anonymize user`. Installed from source at build time.
./plugins/autocreate
./plugins/anonymize
