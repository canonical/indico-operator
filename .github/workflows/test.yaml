name: Tests
defaults:
  run:
    shell: bash -leo pipefail {0}

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
    permissions:
      contents: read
      pull-requests: write
