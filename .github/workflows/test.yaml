name: Unit and integration tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@add-machine-charm-support
    secrets: inherit
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@add-machine-charm-support
    secrets: inherit
