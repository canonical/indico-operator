name: Auto-Update Copilot Collections
on:
  workflow_dispatch: # Manual trigger is best for testing

jobs:
  check-update:
    uses: canonical/copilot-collections/.github/workflows/auto_update_collections.yaml@v0.1.2
    with:
      config_file: ".collections-config.yaml"
    secrets: inherit
