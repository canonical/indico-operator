name: Auto-Update Copilot Collections
on:
  schedule:  
    - cron: '0 9 * * 1' # Run every Monday at 09:00 UTC  
  workflow_dispatch:

jobs:
  check-update:
    uses: canonical/copilot-collections/.github/workflows/auto_update_collections.yaml@v0.1.3
    with:
      config_file: ".collections-config.yaml"
    secrets: inherit
