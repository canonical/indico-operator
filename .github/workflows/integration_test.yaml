name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@ISD-375-trivy
    secrets: inherit
    with:
      chaos-enabled: false
      chaos-experiments: pod-delete
      chaos-app-label: app.kubernetes.io/name=indico
      load-test-enabled: false
      trivy-fs-enabled: true
